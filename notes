Play
  Keep playing while navigating!
  disable prev and next if no prev or next track
  keyboard listeners, playbar listeners

Track show
  clicking on track name or image links here
  create redirects here
  add public/private

Profile
  Add bio
  Make sure editing updates the page!
  conditional for showing full name

Display errors on forms
  -- submit function triggers action to clear message store
  -- ajax errors trigger action to send messages to store
  -- modals listen to the message store (this.state.errors)
  -- Error component passed this.state.errors as props
  -- ul with li of props (list-style bullet)

_________________

Likes -
  Tracks load with like count (update as like button pressed)
  Users load with count of tracks likes
  Like index shows a given user's liked tracks (like store)

Follows
  Users load with count of followers and followees
  Stream indexes followees tracks (track store)
  Follower and Followee store (user store)

Playlists
  Playlist store
  Incorporate playlists into stream?

-----------------

Audio
  Spinner while waiting for upload to complete?
  validate type
  get rid of default

image
  paperclip image sizes
  cropping?

nav
  Highlight current tab in navbar
  redirect to user's desired location after login
  scroll to top whenever you navigate to new page

Assorted
  Edit and Delete icons
  Reset guest tracks on logout?
  validate username has only letters, number, -
  navbar and modals f-ed up when resizing window... (media query)
  more efficient FormData appending?
  Background image or color choice?
