Fix naming of component _functions

Forms -
  Upload form (cf audio upload modal, and include images, audio, etc.)
  Recycle track and user forms where possible - make create user modal

  Bug - enter to submit user form (also check login forms)
  validate audio type
  validate username has only letters, number, -

----------------------------

Play
  Toggle play and pause button
  Stop play of deleted tracks
  Stop track play on signout?

Explore
  gets its own index with thumbs
  (filter out current_user in the index)

Navbar
  upload link
  logo links to stream (for now explore)
  clone sheep!

Track show
  clicking on track name or image links here
  add public/private

Clean up CSS
**** buttons

Fix delete dialog! -> add to a dialog store, have index register to rerender (not index item)
Display errors on forms -> let me guess... error store!

**** navbar and modals f-ed up when resizing window...

Message and FormError stores
